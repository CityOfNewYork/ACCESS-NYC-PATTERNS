/ Layout
= extend('layouts/default');

/ Component
- title = 'Nearby Stops';

/ Partials
= partial('partials/styles.slm');
= partial('partials/links.slm');
= partial('partials/head.mixin.slm');
= partial('partials/content-header.mixin.slm');
= partial('partials/section.mixin.slm');
= partial('partials/section-text.mixin.slm');
= partial('partials/demo-specs.mixin.slm');
= partial('partials/demo-block.mixin.slm');

/ Content blocks
= content('head');
  = mixin('head', title);

= content('header');
  = mixin('header', title);

= content('content');
  = mixin('content-header', title);
  = mixin('section', 'About', 'components/nearby-stops/nearby-stops');
  = mixin('section-text', 'Develop', 'components/nearby-stops/nearby-stops-develop');

= content('script');
  javascript:
    access.nearbyStops();
